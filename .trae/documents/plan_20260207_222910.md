我将执行以下步骤来优化搜索栏样式、补充图书封面并提供测试账号：

1. **优化搜索栏样式 (Vue)**

   * **目标文件**: `libra-frontend/src/views/user/Home.vue`

   * **修改方案**:

     * **移除**原有的 `<template #append>`（这种拼接方式容易导致圆角和边框对齐问题）。

     * **采用**“胶囊式”设计：将输入框改为全圆角（Pill shape），并使用绝对定位将“搜索”按钮放置在输入框内部右侧。

     * 增加阴影和悬浮效果，使其看起来更现代、流畅。

2. **补充图书封面 (SQL)**

   * **目标文件**: `libra-admin/src/main/resources/db/h2-seed.sql`

   * **操作**:

     * 更新种子数据，为 4 本现有图书添加高清封面图片链接（使用豆瓣源）。

   * **为您提供的 SQL**: 我将在执行完成后，直接提供一组 \`

