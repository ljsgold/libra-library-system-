收到，原来技能文件已经存在于项目中。因为我当前的运行环境没有自动加载该路径下的技能，所以我需要手动读取它来获取您的“Pro Max”设计规范。

### 计划：加载并应用本地 Skill 规范

1.  **定位技能文件**
    *   扫描 `E:\图书管理系统\.cursor\skills` 目录，确认 `ui-ux-pro-max` 的具体文件名（如 `.md` 或 `.json`）。
2.  **读取并解析规范**
    *   读取该文件内容，提取核心设计原则（色彩系统、阴影参数、圆角规则、交互动画等）。
3.  **严格执行优化**
    *   根据提取的规范，重新校对 `Home.vue` 的搜索栏设计。
    *   将这套规范应用到 `BookList.vue` 等其他页面，确保完全符合您定义的“UI-UR-ProMax”标准。

我现在就去读取这个文件。